# Go
*.exe
*.exe~
*.dll
*.so
*.dylib
bin/
dist/
vendor/

# Node
node_modules/
.pnpm-store/
apps/web/dist/
apps/web/.vite/
*.log

# Stray runtime log dumped at repo root (e.g. captured server stdout)
/log.txt

# Environment
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Build artifacts
apps/api/internal/store/gen/
*.pb.go

# Data
data/
postgres-data/
redis-data/

# SQLite (dev DB is regenerated on boot)
*.db
*.db-wal
*.db-shm

# Auto-generated app-secret encryption key (never commit)
.mcsm-secret-key

# Per-server working dirs (created by agent at runtime).
# Anchored to repo root so it does NOT match apps/web/src/routes/servers/.
/servers/

# Build caches
apps/web/tsconfig.tsbuildinfo
.codex/
.codex-go-cache*/

# Local design/animation scratchpad (not part of the app build)
/tools/

# Local agent / dev-tool dirs (never commit)
/.claude/
/.codex-bin/
/.logs/
/.bin/
